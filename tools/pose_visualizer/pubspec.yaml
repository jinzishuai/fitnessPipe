name: pose_visualizer
description: Tool to visualize pose data from test fixtures
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  fitness_counter:
    path: ../../packages/fitness_counter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
