# Claude Code Instructions

## Before committing code changes

- Run `dart format lib/ test/ packages/` to format Dart code
- Run `dart analyze` to check for issues
- Ensure both pass before committing
